const form=document.getElementById('loginForm'), msg=document.getElementById('loginMsg');
form.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='Authenticating…';const b=Object.fromEntries(new FormData(form).entries());try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const j=await r.json();if(!r.ok)throw new Error(j.error||'Sign in failed');sessionStorage.setItem('atlas_csrf',j.csrf_token);location.href='/app/';}catch(err){msg.textContent=err.message;msg.style.color='#ff8996'}});
